const base = '.calculator';
const display = `${base}__display`;
const buttons = `${base}__numpad-button`;
const buttonInner = `${buttons}--inner`;
const buttonControl = `${buttons}--operator`;


export default {
  cssClasses: {
    display,
    buttons,
    buttonInner,
    buttonControl
  }
};